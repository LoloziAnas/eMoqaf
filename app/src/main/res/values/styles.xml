<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Style for a tab that displays a category name -->
    <style name="CategoryTab" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@color/colorAppBarText</item>
        <item name="tabSelectedTextColor">@color/colorAppBarText</item>
        <item name="tabTextAppearance">@style/CategoryTabTextAppearance</item>
    </style>

    <!-- Text appearance style for a category tab -->
    <style name="CategoryTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textColor">@color/colorTabText</item>
    </style>
    <style name="inputRadius" parent="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox.Dense">
        <item name="boxCornerRadiusBottomStart" >@dimen/radius20</item>
        <item name="boxCornerRadiusBottomEnd" >@dimen/radius20</item>
        <item name="boxCornerRadiusTopStart" >@dimen/radius20</item>
        <item name="boxCornerRadiusTopEnd" >@dimen/radius20</item>
        <item name="android:layout_marginTop">20dp</item>
    </style>
    <style name="connexionBtn" parent="@style/Widget.MaterialComponents.Button.OutlinedButton">
        <item name="backgroundTint">#CCEAFF</item>
        <item name="android:bottomLeftRadius">50dp</item>
        <item name="android:bottomRightRadius">50dp</item>
        <item name="android:topLeftRadius">50dp</item>
        <item name="android:topRightRadius">50dp</item>
        <item name="cornerRadius">50dp</item>

    </style>

</resources>
